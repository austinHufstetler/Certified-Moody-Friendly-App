<h2>Upcoming Events</h2>
<ul>
	<% @events.each do |event| %>
		<li>
			<%= image_tag(event.image_url) %>
			<h2><%= event.title %></h2>
			<p>
				<%= sanitize(event.description) %>
			</p>
		</li>
	<% end %>
</ul>